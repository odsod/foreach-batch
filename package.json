{
  "name": "foreach-batch",
  "version": "0.0.0",
  "description": "Deferred batch processing to keep the UI responsive under intensive computations.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
