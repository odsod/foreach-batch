{
  "name": "foreach-batch",
  "version": "0.0.1",
  "description": "Deferred batch processing to keep the UI responsive under intensive computations.",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha --reporter list --no-colors"
  },
  "author": "Oscar Söderlund",
  "license": "MIT",
  "devDependencies": {
    "mocha": "~1.18.2"
  }
}
